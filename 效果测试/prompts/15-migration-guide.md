# 测试输入：Migration Guide

## 用户请求

```
请写一份从 v1 REST API 迁移到 v2 GraphQL API 的迁移指南。

要求：
1. 先读取 2-3 个真实迁移指南案例，例如 UI 框架、路由库、API/SDK 或配置格式迁移。
2. 迁移指南中的结构可以参考案例，但具体 REST/GraphQL 内容只能基于本 prompt 的事实合理展开。
3. 不要虚构 GA 日期、下线日期、自动迁移命令、环境变量或兼容周期。
4. 缺少产品事实的地方写成 `TODO` 或“待确认”。
```

## 期望场景

使用 oh-my-gh-writing 的 Migration Guide 场景。
