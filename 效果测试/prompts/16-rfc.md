# 测试输入：RFC

## 用户请求

```
提议在 TypeScript 中增加 "运算符重载" 功能，
允许开发者为自定义类型定义 + - * / 等运算符的行为。

要求：
1. 先读取 TypeScript 相关 issue/proposal 或讨论资料。
2. 再读取 2-3 个 prior art，例如 Python、Rust、Kotlin、C++ 或 TC39 相关提案。
3. 把设计写成 RFC 草案，不要把编译策略、运行时兼容性或标准状态写成既定事实。
```

## 期望场景

使用 oh-my-gh-writing 的 RFC 场景。
