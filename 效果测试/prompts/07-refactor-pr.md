# 测试输入：Refactor PR

## 用户请求

```
我把项目的工具函数从分散的文件合并到了统一的 utils/ 目录下。
没有改动任何外部行为，只是调整了 import 路径。

要求：
1. 如果测试时能找到或构造真实 diff，先读取 diff 再写 PR 描述。
2. 没有 diff 时，不要虚构文件数量、测试数量、导入路径或构建结果。
3. 行为不变声明要写成基于用户说明的声明，并列出待运行验证。
```

## 期望场景

使用 oh-my-gh-writing 的 Refactor PR 场景。
