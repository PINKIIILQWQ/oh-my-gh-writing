# 测试输入：Feature Request

## 用户请求

```
我希望 Prisma ORM 支持 PostgreSQL 的 PARTITION BY 子句，
这样我可以在数据库层面做时间分区，而不需要在应用层手动处理。
目前 Prisma 的 @map 和 @@map 无法满足这个需求。
```

## 期望场景

使用 oh-my-gh-writing 的 Feature Request 场景。
