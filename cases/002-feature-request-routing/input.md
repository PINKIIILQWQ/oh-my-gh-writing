# Input

Turn this postmortem note into a feature request, not a PR: a previous PR added a manual export workaround; now we want first-class scheduled CSV exports with clear delivery settings.

## What This Case Tests

- Routes by requested artifact, not source material.
- Produces Feature Request, not Feature PR.
- Uses the previous PR only as context.
